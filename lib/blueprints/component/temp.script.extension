{% if filesType.script === 'tsx' %}
import React from 'react';
import './{{name | fileNameCase}}.module.css';

interface {{name | fileNameCase}}Props extends React.PropsWithChildren {
  // define expected props here
};

const {{name | PascalCase}}:React.FC<{{name | fileNameCase}}Props> = (props) => {
    return (
      <p className='{{name | kebabCase}}'>Hello from {{name | PascalCase}} component</p>
    );

}

export default {{name | PascalCase}};

{% endif %}
